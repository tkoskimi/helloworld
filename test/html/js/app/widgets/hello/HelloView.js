/**
 * @module test/html/js/app/widgets/helloworld
 */
define( [ "widgets/hello/HelloView" ], function() {
	"use strict";

	describe( "Hello", function() {
		it( "Should show a given text when pressing validate button" );
	} );
} );
